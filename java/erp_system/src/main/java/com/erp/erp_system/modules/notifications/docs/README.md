# Notifications Module

Generates the salary credited message when payroll is approved and attempts to send it
with Spring Mail.

Each notification is stored with the recipient email, message text, send flag, and
creation time for audit visibility.
